<%= form_for(@call_finish) do |f| %>
  <% if @call_finish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@call_finish.errors.count, "error") %> prohibited this call_finish from being saved:</h2>

      <ul>
      <% @call_finish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_call %><br>
    <%= f.number_field :id_call %>
  </div>
  <div class="field">
    <%= f.label :id_user %><br>
    <%= f.number_field :id_user %>
  </div>
  <div class="field">
    <%= f.label :observation %><br>
    <%= f.text_area :observation %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
